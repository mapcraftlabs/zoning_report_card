name: Keep App Warm
on:
  schedule:
    - cron: '0 * * * *' # Once an hour
  workflow_dispatch:
jobs:
  keep_warm:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: curl -s -L -o /dev/null -w "%{http_code}" "https://zoning-report-card.streamlit.app/"
